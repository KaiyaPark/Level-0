// Copyright Wintriss Technical Schools 2013
/*import javax.swing.JOptionPane;

import java.awt.Color;

import org.teachingextensions.logo.Colors;
import org.teachingextensions.logo.Tortoise;
import org.teachingextensions.logo.Turtle.Animals;

public class unicorn {
	public static void main(String[] args) {
		// 1. make the tortoise draw a shape (this will take more than one line
		// of code)

		Tortoise.setAnimal(Animals.Unicorn);
		// 2. set the pen width to 10
		Tortoise.setPenWidth(10);
		Tortoise.setSpeed(10);
		// 3. ask the user what color they would like the tortoise to draw
		String color = JOptionPane.showInputDialog("What color whould you like? turquoise, pink, or plum? Make sure all colors are spelled correctly and lowercase please.");
		// 4. use an if/else statement to set the pen color that the user
		// requested
if (color .equals("turquoise")) { 
	Tortoise.setPenColor(Colors.Greens.Turquoise);
	for (int i = 0; i < 5; i++) {
		Tortoise.move(50);
		Tortoise.turn(90);
	}
	
}
else if (color .equals("pink")) { 
	Tortoise.setPenColor(Colors.Pinks.Pink);
	for (int i = 0; i < 5; i++) {
		Tortoise.move(50);
		Tortoise.turn(90);
	}
	
}
else if (color .equals("plum")) { 
	Tortoise.setPenColor(Colors.Purples.Plum);
	for (int i = 0; i < 5; i++) {
		Tortoise.move(50);
		Tortoise.turn(90);
	}
	
}
else{ 
	Tortoise.setPenColor(Colors.Blues.PaleTurquoise);
	for (int i = 0; i < 5; i++) {
		Tortoise.move(50);
		Tortoise.turn(90);
	}
	
}
				

// 5. if the user doesn’t enter anything, choose a random color

		// 6. put a loop around your code so that you keep asking the user for
		// more colors & drawing them

	}
}
*/
